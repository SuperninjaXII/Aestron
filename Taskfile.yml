version: '3'

tasks:
  default:
    cmds:
      - air
  dev:
    cmds:
      - air
  setup:
    cmd:
      - go install github.com/air-verse/air@latest
      - go install github.com/a-h/templ/cmd/templ@latest
      - go mod tidy
    
